<%= provide(:title, 'Sign Up') %>

<h2>Sign up</h2>

<%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
  <%= devise_error_messages! %>

    <!-- Use the details int this SO to link users and user details:
      http://stackoverflow.com/questions/1231608/rails-user-input-for-multiple-models-on-a-single-form-how
      http://stackoverflow.com/questions/3546289/override-devise-registrations-controller
    -->
    <% f.fields_for :user_detail, UserDetail.new  do |user_detail_form| %>
        <div class="field">
          <%= user_detail_form.label :first_name %><br />
          <%= user_detail_form.text_field :first_name, autofocus: true %>
        </div>

        <div class="field">
          <%= user_detail_form.label :last_name%><br />
          <%= user_detail_form.text_field :last_name %>
        </div>
    <% end %>

  <div class="field">
    <%= f.label :email %><br />
    <%= f.email_field :email %>
  </div>

  <div class="field">
    <%= f.label :password %>
    <% if @validatable %>
    <em>(<%= @minimum_password_length %> characters minimum)</em>
    <% end %><br />
    <%= f.password_field :password, autocomplete: "off" %>
  </div>

  <div class="field">
    <%= f.label :password_confirmation %><br />
    <%= f.password_field :password_confirmation, autocomplete: "off" %>
  </div>

  <div class="actions">
    <%= f.submit "Sign up" %>
  </div>
<% end %>

<%= render "devise/shared/links" %>
